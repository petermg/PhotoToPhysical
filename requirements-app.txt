# Direct dependencies used by the PhotoToPhysical Gradio application.
#
# PyTorch / torchvision / xformers are intentionally installed separately
# from the PyTorch CUDA 12.8 index; see INSTALL_WINDOWS_NVIDIA.txt.
#
# Depth Anything 3 is intentionally installed from the pinned upstream commit
# documented in README.md / INSTALL_WINDOWS_NVIDIA.txt.
#
# Optional components are intentionally NOT included here:
# - gsplat: only needed for DA3 Gaussian preview-video rendering; source-built
#   using the exact DA3-pinned commit and --no-build-isolation.
# - OpenMVS: external executable backend.
# - 2DGS: separate external Python/CUDA environment.

gradio==6.25.0
numpy==1.26.4
open3d==0.19.0
opencv-python==4.11.0.86
Pillow==12.3.0
trimesh==5.0.0
